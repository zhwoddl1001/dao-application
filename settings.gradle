pluginManagement {
    repositories {
        maven { url 'https://repo.spring.io/snapshot' }
        gradlePluginPortal()
    }
    plugins {
        id 'org.jetbrains.kotlin.jvm' version '2.1.0'
    }
}
rootProject.name = 'dao-application'
